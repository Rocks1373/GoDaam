GoDam — Google Drive setup (OAuth user My Drive)
================================================

Root folder ID (GOOGLE_DRIVE_ROOT_FOLDER_ID) — must be inside the connected user's My Drive:
  174hT47E4QMfTekmpgTi-5xNVUtD0gOHJ

[ ] 1. Google Cloud → Drive API enabled
[ ] 2. OAuth consent screen configured (add test users if External)
[ ] 3. OAuth Web client (same project as GOOGLE_DRIVE_OAUTH_CLIENT_ID) — Authorized redirect URIs (add BOTH if unsure):
      http://localhost:3001/api/google/oauth/callback
      http://localhost:3001/api/storage/google-oauth/callback
      (production: https://godam.divadivya.cloud/api/google/oauth/callback)
      (production alias: https://godam.divadivya.cloud/api/storage/google-oauth/callback)
[ ] 4. backend/.env:
      GOOGLE_DRIVE_AUTH_MODE=oauth
      GOOGLE_DRIVE_OAUTH_CLIENT_ID=...apps.googleusercontent.com
      GOOGLE_DRIVE_OAUTH_CLIENT_SECRET=...
      GOOGLE_DRIVE_ROOT_FOLDER_ID=...
      FRONTEND_URL=http://localhost:5173
[ ] 5. Restart backend
[ ] 6. Web → Admin → Google Drive → Connect Google Drive → Test upload
[ ] 7. Outbound / Sales Order Documents uploads should succeed to Drive

Legacy service account (optional): GOOGLE_DRIVE_AUTH_MODE=service_account + JSON path.
