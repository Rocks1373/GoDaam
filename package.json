{
  "name": "warehouse-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:all": "concurrently -n backend,frontend,mobile -c green,cyan,magenta \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\" \"npm run start --prefix godam-mobile\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "seed:sample": "npm run seed:sample --workspace backend",
    "seed:test-data": "npm run seed:test-data --workspace backend",
    "android:emu": "bash scripts/run-android-emulator.sh",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d --build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
