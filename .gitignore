node_modules
.venv/
venv/
.env
.env.*
!.env.example
!**/.env.example
# Google OAuth client secrets (never commit)
client_secret*.json
# Google service account keys (never commit; use GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON_PATH outside repo in prod)
godam-*.json
godaam-*.json
dist
build
.expo
.expo-shared
*.log
# MS Office Excel temp lock files (never commit)
~$*
*.sqlite
*.db
*.sqlite-journal
uploads/
coverage/
.DS_Store
.vscode/
.idea/
.claude/

# Editor/AI backup files — never commit
*.bak
*.bak.ai
*.orig
*.rej

# Production-ish files that should never be in repo
backend/backups/
**/warehouse.db*
**/huawei_godam.db*

# Large APK hosted next to web app but not stored in Git (deploy via rsync/scp)
frontend/public/downloads/*.apk
releases/*.apk

# VPS-pulled secrets + mirrored test mobile tree (scripts/test-mobile/setup-from-vps.sh)
/test/godam-mobile/
/test/.pulled-backend.env
/test/GoDam-test-release.apk

# Local-only exports, temp tool output, and ad-hoc documents
local-nonessential-archive/

# Local tooling / logs
.godam/
*.pid

# Compiled class file
*.class

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*
